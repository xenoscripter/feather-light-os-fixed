# Feather Light OS Fixed

Clean rebuild focused on reliable BIOS/UEFI and Ventoy/SSD boot.

The first build uses Alpine Linux's tested kernel/initramfs and adds explicit boot choices:

- Normal Boot
- Safe Hardware Mode
- Safe Graphics Mode
- Recovery Shell

The default entry intentionally uses no experimental PCI parameters.
